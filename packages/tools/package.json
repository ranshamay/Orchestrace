{
  "name": "@orchestrace/tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "pretypecheck": "pnpm --filter @orchestrace/provider build && pnpm --filter @orchestrace/context build && pnpm --filter @orchestrace/tools build",
    "typecheck": "tsc --noEmit",
    "pretest": "pnpm --filter @orchestrace/provider build && pnpm --filter @orchestrace/context build && pnpm --filter @orchestrace/tools build",
    "test": "vitest run --config vitest.config.ts",
    "clean": "rm -rf dist"
  },
    "dependencies": {
    "@mariozechner/pi-ai": "^0.64.0",
    "@orchestrace/context": "workspace:*",
    "@orchestrace/core": "workspace:*",
    "@orchestrace/provider": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
