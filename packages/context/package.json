{
  "name": "@orchestrace/context",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "pretypecheck": "pnpm turbo run build --filter=@orchestrace/context...",
    "typecheck": "tsc --noEmit",
    "pretest": "pnpm turbo run build --filter=@orchestrace/context...",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@orchestrace/provider": "workspace:*",
    "js-tiktoken": "^1.0.18"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
