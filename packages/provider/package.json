{
  "name": "@orchestrace/provider",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "pretypecheck": "pnpm --filter @orchestrace/provider... build",
    "typecheck": "tsc --noEmit",
    "pretest": "pnpm --filter @orchestrace/provider... build",
    "test": "vitest run",
    "clean": "rm -rf dist"
  }
  "dependencies": {
    "@mariozechner/pi-ai": "^0.64.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
