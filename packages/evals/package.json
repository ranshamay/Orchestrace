{
  "name": "@orchestrace/evals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "pretypecheck": "pnpm --filter @orchestrace/evals... build",
    "typecheck": "tsc --noEmit",
    "pretest": "pnpm --filter @orchestrace/evals... build",
    "test": "vitest run",
    "run": "tsx src/run-evals.ts",
    "clean": "rm -rf dist"
  }
  "dependencies": {
    "@orchestrace/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
