{
  "name": "@orchestrace/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "pretypecheck": "pnpm --filter @orchestrace/core... build",
    "typecheck": "tsc --noEmit",
    "pretest": "pnpm --filter @orchestrace/core... build",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@orchestrace/provider": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
